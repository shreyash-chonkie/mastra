---
title: "リファレンス: Workflow.commit() | ワークフローの構築 | Mastra ドキュメント"
description: ワークフロー内の `.commit()` メソッドのドキュメント。ワークフローを確定し、最終結果を返します。
---

# Workflow.commit()

`.commit()` メソッドはワークフローを確定し、最終的な結果を返します。

## 使い方

```typescript
workflow.then(stepOne).commit();
```

## 戻り値

<PropertiesTable
  content={[
    {
      name: "workflow",
      type: "Workflow",
      description: "メソッドチェーン用のワークフローインスタンス",
    },
  ]}
/>
