---
title: "リファレンス: Workflow.resume() | ワークフローの構築 | Mastra ドキュメント"
description: ワークフロー内の `.resume()` メソッドのドキュメント。新しいデータで一時停止中のワークフロー実行を再開します。
---

# Workflow.resume()

`.resume()` メソッドは、一時停止されたワークフローの実行を新しいデータで再開し、特定のステップから処理を続行できるようにします。

## 使い方

```typescript
const run = counterWorkflow.createRun();
const result = await run.start({ inputData: { startValue: 0 } });

if (result.status === "suspended") {
  const resumedResults = await run.resume({
    step: result.suspended[0],
    resumeData: { newValue: 0 },
  });
}
```

## パラメーター

<PropertiesTable
  content={[
    {
      name: "params",
      type: "object",
      description: "ワークフロー実行の再開用設定オブジェクト",
      isOptional: false,
      properties: [
        {
          name: "resumeData",
          type: "ResumeSchema",
          description: "一時停止されたステップを再開するためのデータ",
          isOptional: true,
        },
        {
          name: "step",
          type: "Step | Step[] | string | string[]",
          description: "実行を再開するステップ",
          isOptional: false,
        },
        {
          name: "runtimeContext",
          type: "RuntimeContext",
          description: "再開時に使用するランタイムコンテキストデータ",
          isOptional: true,
        }
      ]
    },
  ]}
/>

## 戻り値

<PropertiesTable
  content={[
    {
      name: "resumedResults",
      type: "Promise<WorkflowResult<TOutput, TSteps>>",
      description: "再開されたワークフロー実行の結果を解決するPromise",
    },
  ]}
/>
